- 批注2

将representation learning;TSK fuzzy system改为 fuzzy system

- 批注3

按照参考意见

- 批注4

  (2.2a)改为(2)

- 批注5

,从而获得分类结果不同->从而获得分类结果的方式不同

- 批注6

(3.4)改为(18)

- 批注7

l下标改为S

- 批注8

在每个区域上做直方图统计范围[]内每个整数出现的次数

改为

在每个区域内做直方图统计,统计范围[]内每个整数出现的次数

- 批注9

固定子空间维度数目->其子空间维度数固定

- 批注10

DBM改为(deep boltzmann machines, DBM)

注意不要删掉参考文献链接

格式参考这样

![image-20201125095058492](https://notepic-1302850888.cos.ap-nanjing.myqcloud.com/img/image-20201125095058492.png)

- 批注11

IFL-FR改为(image feature learning with fuzzy rule system, IFL-FR)







```
@Override
public void onParkingGear(int value) {
    //驻车档
    Log.d(TAG, "onParkingGear value: " + value);
    if (value == Constants.ON) {
        switchGear(LauncherApplication.Status.GEAR_PARK);
    }
}

@Override
public void onSpeedGear(int value) {
    //运动档
    Log.d(TAG, "onSpeedGear value: " + value);
    if (value == Constants.ON) {
        switchGear(LauncherApplication.Status.GEAR_SPORT);
    }
}

@Override
public void onSeatSense(int value) {

}

@Override
public void onCruiseMode(int value) {

}

@Override
public void onBackCar(int value) {
    //倒车档
    Log.d(TAG, "onBackCar value: " + value);
    if (value == Constants.ON) {
        switchGear(LauncherApplication.Status.GEAR_REVERSE);
    }

}
```





```
private void switchGear(LauncherApplication.Status status) 
```

```
     mApplication.setStatus(status);
        switch (status){
            //驻车档,橙色
            case GEAR_PARK:
                replaceImgSrc(R.drawable.bg_orange, R.drawable.ic_bg_bar_orange, R.drawable.pop_p, R.drawable.ic_gear_p);
                break;
            //节能档,蓝色
            case GEAR_ECO:
                replaceImgSrc(R.drawable.bg_blue, R.drawable.ic_bg_bar_blue, R.drawable.pop_e, R.drawable.ic_gear_s);
                break;
                //运动档,蓝色
            case GEAR_SPORT:
                replaceImgSrc(R.drawable.bg_blue, R.drawable.ic_bg_bar_blue, R.drawable.pop_e, R.drawable.ic_gear_s);
                break;

                //倒车档,黄色
            case GEAR_REVERSE:
                replaceImgSrc(R.drawable.bg_yellow, R.drawable.ic_bg_bar_blue, R.drawable.pop_r, R.drawable.ic_gear_r);
                break;
            case GEAR_LOCKED:
                popLockedDialog(mDialog);
                break;
            case GEAR_ALARM:
                popAlarmDialog(mDialog);
                break;
            default:
                break;
        }
```



```
         case "01":
                mEditor.putString(str,"扳手故障");
                break;
            case "02":
                mEditor.putString(str,"转把故障");
                break;
            case "03":
                mEditor.putString(str,"电机故障");
                break;
            case "04":
                mEditor.putString(str,"BMS通讯故障");
                break;
            case "05":
                mEditor.putString(str,"MCU通讯故障");
                break;
            case "06":
                mEditor.putString(str,"充电器通讯故障");
                break;
            case "07":
                mEditor.putString(str,"上位机通讯故障");
                break;
```



```
     @Override
        public void onBlacklightFault(int value) {
            // 背光灯故障
            Log.d(TAG, "onBlacklightFault: "+value);
            if(value == 1){
                addFaultInfo("BlacklightFault");
            }
        }

        @Override
        public void onEepromFault(int value) {
            // EEPROM故障
            Log.d(TAG, "onEepromFault: "+value);
            if(value == 1){
                addFaultInfo("EepromFault");
            }
        }

        @Override
        public void onLightSensorFault(int value) {
            // 阳光传感器故障
            Log.d(TAG, "onLightSensorFault: "+value);
            if(value == 1){
                addFaultInfo("LightSensorFault");
            }
        }

        @Override
        public void onLeftLedFault(int value) {
            // 左转向灯故障
            Log.d(TAG, "onLeftLedFault: "+value);
            if(value == 1){
                addFaultInfo("LeftLedFault");
            }
        }

        @Override
        public void onRightLEDFault(int value) {
            // 右转向灯故障
            Log.d(TAG, "onRightLEDFault: "+value);
            if(value == 1){
                addFaultInfo("RightLEDFault");
            }
        }
```

